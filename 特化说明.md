# 场景特化说明

本扩展的默认路径是：在页面上寻找「下一页 / 播放」这类通用按钮并模拟点击。  
有些站点的按钮是图片、写在跨域 iframe 里，或必须走页面自己的脚本才算一次有效操作。通用识别在这些页面上不够用，因此会为**特定场景**单独写适配。

特化只在匹配到该场景时启用；其它网站仍走通用逻辑。  
**目前已完成：课件类页面。** 后续可能再增加其它场景，届时会写在本文，而不会把细节散落到 README / 安装说明里。

---

## 已完成：课件类页面

针对带「一节一页」结构的 H5 课件（常见于学习平台把课件嵌在跨域 iframe 里，例如外层站点 + `mcwk.mycourse.cn` / `mycourse.cn/course/` 内的内容页）。

### 识别条件

同时（或接近同时）满足时走课件特化，而不是把外层壳页面当成翻页目标：

- 文档里有 `.page-item` / `.page-WH.page-item`
- 当前节带 `.page-active`
- 当前节内有 `.btn-next` / `.btn-prev` / `.btn-start` 等（可以是隐藏的图片按钮）

外层壳（如 `weiban.mycourse.cn`）本身通常没有这些节点，不会被当成课件 frame。

### 普通翻页（优先）

1. 顶层遥控把「上一页 / 下一页」发给各 frame  
2. 课件页的**主世界**脚本（`content/course-bridge.js`）在当前 `.page-item.page-active` 里找到按钮  
3. 用页面自己的 `jQuery(按钮).trigger("click")` 点击  

这样会走课件原来的点击逻辑（计数、接口、切 `page-active` 等）。隔离世界里对 `javascript:;` 链接触发的点击经常无效，所以这一步必须在主世界做。

### 强制执行（仅提示条里的红按钮）

找不到按钮、或主世界点击失败时，**不会**因为连点遥控而强制。只有提示里的「强制执行（有风险）」才会：

- 把 `page-active` 从当前节挪到相邻的 `.page-item`

画面会翻，但会跳过站点自己的点击回调。  
**使用强制跳过可能存在风险，请谨慎使用。** 详见 README 中的警告。

### 使用注意

- 扩展「网站访问」必须设为**在所有网站上**。只允许「单击时」，进不了跨域课件 iframe，特化会失败。  
- 改完代码后：扩展页点「重新加载」，课件标签**关掉再打开**，iframe 才会拿到新脚本。  
- 隐藏着的下一页按钮也会点（很多课件把按钮做成看不见的图）。

### 维护备忘

| 现象 | 原因 | 处理 |
|---|---|---|
| 强制 `not_found` 且 `course=0` | 进不了课件 iframe | 网站访问 = 所有网站 |
| `Cannot access contents of the page` | 主世界注入被拒 | 同上；隔离世界改 class 仍可试 |
| 画面翻了但站点状态没跟上 | 走了强制改 DOM | 用普通下一页，走页面原点击 |
| 外层壳被当成课件 | 用错了 hostname | 只认 `mcwk` 或路径 `/course/` |

相关实现：`content/course-bridge.js`、`background.js` 的 `courseNavInPage` / `UR_PAGE_ITEM`、`content.js` 里的 `applyPageItemNav`。

---

## 以后要加新特化时

1. 只在本文写该场景的识别条件、走哪条路径、和通用逻辑的差异  
2. README / 安装说明只保留一句「部分场景有特化，详见本文」，不要再抄细节  
3. 不要把某个特化改成默认强制；找不到按钮时仍须用户确认  
4. 在 `工程说明.md` 的目录里加一行指向新文件或本节即可  
