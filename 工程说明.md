# 网页万能遥控 · 工程说明

Edge / Chromium MV3 扩展。页面浮层遥控器：音视频快进快退、播放暂停、上一页 / 下一页。  
优先点页面原有按钮；找不到时由用户在提示里点「强制执行」，**连点遥控按钮不会强制**。

当前版本见 `manifest.json` 的 `version`，浮层标题也会显示 `vX.Y.Z`。

部分站点会走**场景特化**（目前：课件类页面）。识别条件与实现见 [特化说明.md](./特化说明.md)，本文不再展开。

---

## 1. 目录

```
网页万能遥控/
  manifest.json          扩展清单（改完必须在 edge://extensions 点重新加载）
  background.js          Service Worker：frame 登记、跨 frame 翻页 / 音视频
  content/
    content.js           主内容脚本（隔离世界）：检测、浮层、消息、长连接
    overlay.css          浮层样式（注入 Shadow DOM，不污染页面）
    css-anim.js          试验：CSS 动画加速/跳过（可整段删除）
    course-bridge.js     课件特化【主世界】：见 特化说明.md
  icons/                 16/32/48/128 PNG
  demo.html              本地演示页
  安装说明.txt            给使用者的安装步骤
  特化说明.md             各场景特化（课件等）
  工程说明.md             本文，给维护者
```

改代码后：**重新加载扩展**。含 iframe 的页面要**关掉标签再打开**，子 frame 才会拿到新脚本。

---

## 2. 运行时结构

```
标签页顶层
  └─ content.js（隔离世界）→ 画出遥控浮层
  └─ 子 frame / iframe
        └─ content.js（隔离世界，无浮层）
        └─ 若命中某项特化，再注入对应的主世界脚本
```

- 浮层只在 `window === top` 创建。
- 样式在 Shadow DOM 里，选择器都以 `.ur-` 开头。
- 需要调用页面自己的 jQuery / 播放器 API 时，必须在 **MAIN** 世界执行。

---

## 3. 关键交互

### 3.1 普通「上一页 / 下一页」

在当前文档（及可访问的 frame）里按优先级找按钮：已记住的、站点适配选择器、明显的 next/prev 类名、可见文案、分页数字。  
找到后模拟真实点击，走网站原逻辑。

不要把搜索框、收藏、点赞等工具栏控件当成翻页目标。

操作成功后提示条仍提供「尝试其他可能按键」（把刚才那个加入跳过集，改点下一候选）和「改为指定某按键」（进入点选记忆）。自动下一页走这条普通路径，不走强制。

点播放 / 翻页时先弹出「正在尝试」，完成后再换成结果。最小化时隐藏提示。

### 3.4 指定按钮怎么存

`chrome.storage.local` 键 `ur-learned`：

```
{
  "www.example.com/path#hash前两段": { nextPage: rec, prevPage: rec },
  "www.example.com": { nextPage: rec }
}
```

查找顺序：先 `hostname + pathname + 截断后的 hash`（`pageKey`），没有再用纯 `hostname`。  
`rec` 里主要是 CSS 选择器，以及 title / aria-label / 文本，用来在页面上再找到那个元素。

不能指定遥控器自身（含 Shadow DOM 里的控件）；点到遥控会提示「指定失败」并继续等待点网页按钮。指定结束必须提示成功或失败。

「删除本网站所有指定」会删掉 `hostname`、`hostname/...`、`hostname#...` 下的全部记录。

指定时若点击导致跳转：同时写入 `ur-pending-toast`，新页面浮层启动后读出来再提示，并仍可「改为指定」。跳转过程中还由 background 再写一遍 storage，避免内容脚本被卸掉时写入丢失。

**禁止**在 `showToast` 里再写入 `ur-pending-toast`。否则 storage.onChanged → 再显示提示 → 再写入，会在播放/翻页后形成死循环，内存暴涨甚至闪退。提示跨页只由指定结果那一次写入。查找按钮时不要走进遥控器自己的 Shadow DOM，也禁止 `simulateClick` 点到遥控自身。

### 3.2 强制执行（仅提示条红色按钮）

**不会**因为连点遥控「下一页」触发。

强制路径直接改页面结构或状态，**跳过**站点自己的点击回调。仅作找不到按钮时的兜底。  
**开源说明与用户提示均须写明此风险，不得改成默认强制。**

各特化场景的强制细节见 [特化说明.md](./特化说明.md)。

### 3.3 音视频

优先在每个 frame 里找正在播放、面积最大的 `video`/`audio`（含 open Shadow DOM），设 `fastSeek` / `currentTime`，或 `play()` / `pause()`。  
并尝试西瓜播放器 `.xgplayer.player.seek`、YouTube `#movie_player.seekTo`。  
播放/暂停按钮的图标和颜色按**实际媒体状态**更新（含其它 frame）；不要只看顶层文档里有没有 `<video>`。

倍速：`UR_MEDIA` 的 `kind: "rate"`，写 `playbackRate`，并尽量调页面播放器 / YouTube API。

自动下一页：浮层定时调用普通 `execute("nextPage")`，间隔 10–600000ms；失败即停。

### 3.5 CSS 动画加速 / 跳过（试验，可删）

独立文件 `content/css-anim.js`，消息 `UR_CSS_ANIM`。只动 `document.getAnimations()` 能拿到的 CSS / Web Animations，不管音视频、rAF、Canvas。  
加速会改 `playbackRate`（含循环动画）；跳过只 `finish()` 视口内、有限次的动画，避免把无限 loading 掐死。  
「持续检查」在各 frame 里每 100ms 再执行一次上次的倍速或跳过；新 iframe 通过 `UR_FRAME_HELLO` 跟上。隐藏遥控时关闭。  
删除方法见该文件头部注释。代码里凡「CSS anim feature」注释块均可整段去掉。

---

## 4. 模块职责

| 文件 | 世界 | 职责 |
|---|---|---|
| `content.js` | isolated | 按钮检测、浮层、拖拽、toast、port、HELLO |
| `course-bridge.js` | MAIN | 课件特化，见特化说明 |
| `background.js` | SW | `frameBook` / `framePorts`、按 frame 注入、主世界执行 |
| `overlay.css` | Shadow | 遥控外观 |

`background.js` 里传给 `executeScript` 的函数必须**自包含**，不能闭包引用 SW 里的其它函数。

---

## 5. 消息一览

| 方向 | type | 含义 |
|---|---|---|
| iframe → SW | `UR_FRAME_HELLO` | 报到：href、是否命中课件结构 |
| 浮层 → SW | `UR_PAGE_ITEM` | 特化翻页（`dir`, `force`） |
| 浮层 → SW | `UR_MEDIA` | 快进 / 暂停 / 倍速（`kind: rate`） |
| 浮层 → SW | `UR_CSS_ANIM` | 试验：CSS 动画加速 / 跳过 |
| 浮层 → SW | `UR_MEDIA_STATE` | 询问各 frame 当前播放/暂停 |
| 浮层 → SW | `UR_BROADCAST` | 转发到其它 frame |
| 顶层 → iframe | `postMessage` `__ur` | 通知子 frame 执行特化动作 |
| port `ur-frame` | `NAV` / `MEDIA` | 不依赖 frameId 的长连接 |

页面进入往返缓存（bfcache）时必须先 `port.disconnect()`，并在 `onDisconnect` 里读取 `chrome.runtime.lastError`，否则控制台会报 channel closed。

`scripting.executeScript` 进跨域 iframe 可能报必须请求对应 host 权限。  
用户需在扩展详情把「网站访问」设为**在所有网站上**。

---

## 6. 改 UI

- 结构：`content.js` 的 `mount()`
- 样式：只改 `content/overlay.css`，类名一律 `.ur-*`
- 绿点 = 识别到页面按钮；蓝点 = 能直接控播放器
- 播放中：主按钮为绿色并显示暂停图标
- 高级区：`.ur-advanced`，由 `.ur-adv-toggle` 展开
- 强制按钮必须是 toast 里的 `.ur-toast-force`，不要绑回「下一页」
- 成功提示的备选：`.ur-toast-alt`（尝试其他）、`data-capture`（改为指定）
- 最小化条 `.ur-min` 本身是 button，拖拽时不要把「点到 button 就放弃」套在把手自己身上

改完把 `manifest.json` 和 `UR.VERSION` 一起加版本号。

---

## 7. 常见故障

| 现象 | 原因 | 处理 |
|---|---|---|
| 浮层版本不是最新 | 只刷新了网页 | `edge://extensions` 重新加载，含 iframe 的标签关掉重开 |
| 跨域 iframe 里点不动 | 网站访问不是「所有网站」 | 扩展详情改为在所有网站上 |
| 画面翻了但站点状态没跟上 | 走了强制改 DOM | 用普通下一页，走页面原点击 |
| 连点下一页误强制 | 旧版 8 秒内再点会强制 | ≥1.2.5 已取消，只认 toast 红按钮 |
| 播放能控但按钮图标不变 | 媒体在子 frame / 内置播放器里 | ≥1.3.8 会按各 frame 状态刷新按钮 |
| `back/forward cache` lastError | 页面进了往返缓存，port 被关掉 | ≥1.3.8 在 pagehide 时主动断开 |
| 上一页/下一页点到搜索、收藏 | 旧版把工具栏控件当成翻页 | ≥1.3.8 已排除；仍误点可用「尝试其他」或「改为指定」 |
| Chrome 能加载但 iframe 点不动 | 网站访问不是 On all sites | 扩展详情 → 网站访问 → 在所有网站上 |

特化场景的故障见 [特化说明.md](./特化说明.md)。

---

## 8. 安装（开发）

1. `edge://extensions/` → 开发人员模式  
2. 加载解压缩 → 选本文件夹  
3. 网站访问：**在所有网站上**  
4. 打开任意页，左下角应出现 **万能遥控 v…**

`demo.html` 可测识别绿点、隐藏按钮、确认提示。用 `file://` 时需允许访问文件网址。
