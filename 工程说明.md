# 网页万能遥控 · 工程说明

Edge / Chromium MV3 扩展。页面浮层遥控器：音视频快进快退、播放暂停、上一页 / 下一页。  
优先点页面原有按钮；找不到时由用户在提示里点「强制执行」，**连点遥控按钮不会强制**。

当前版本见 `manifest.json` 的 `version`，浮层标题也会显示 `vX.Y.Z`。

---

## 1. 目录

```
网页万能遥控/
  manifest.json          扩展清单（改完必须在 edge://extensions 点重新加载）
  background.js          Service Worker：frame 登记、课件翻页、音视频主世界控制
  content/
    content.js           主内容脚本（隔离世界）：检测、浮层、消息、长连接
    overlay.css          浮层样式（注入 Shadow DOM，不污染页面）
    course-bridge.js     课件页【主世界】脚本：接收 postMessage，点 jQuery 按钮
    actions.js           旧检测模块，现已合并进 content.js，勿当运行入口
    overlay.js           旧浮层模块，现已合并进 content.js，勿当运行入口
  icons/                 16/32/48/128 PNG
  demo.html              本地演示页
  安装说明.txt            给使用者的安装步骤
  工程说明.md             本文，给维护者
```

改代码后：**重新加载扩展**，课件页要**关掉标签再打开**，iframe 才会拿到新脚本。

---

## 2. 运行时结构

```
标签页顶层（如 weiban.mycourse.cn）
  └─ content.js（隔离世界）→ 画出遥控浮层
  └─ iframe.page-iframe → mcwk.mycourse.cn/course/xxxx.html
        ├─ content.js（隔离世界，无浮层）
        └─ course-bridge.js（主世界，能调页面 jQuery）
```

- 浮层只在 `window === top` 创建。
- 样式在 Shadow DOM 里，选择器都以 `.ur-` 开头。
- 课件进度上报必须走**页面主世界**的 `jQuery(按钮).trigger("click")`，隔离世界点 `javascript:;` 经常点不到。

---

## 3. 关键交互

### 3.1 普通「下一页」

1. 外层给所有 iframe `postMessage({ __ur:1, type:"UR_COURSE_NAV", dir })`
2. `course-bridge.js` 在当前 `.page-item.page-active` 里找 `.btn-next` / `.btn-start` 等
3. `jQuery(btn).trigger("click")` → 课件自己的 `pageNums++`、`callApinext`、切 `page-active`
4. 同时 background 可通过 port / `executeScript` 再试一次（主世界）

隐藏按钮也会点（方便测试）。

### 3.2 强制执行（仅提示条红色按钮）

**不会**因为连点遥控「下一页」触发。

强制路径：找到当前 `.page-item.page-active`，把 `page-active` 挪到下一个兄弟节点。  
画面会翻，但**跳过** `callApinext` / `pageNums`，交课进度可能对不上。只给测试兜底。  
**开源说明与用户提示均须写明此风险，不得改成默认强制。**

课件翻页本质：

```js
当前节.next().addClass("page-active").siblings().removeClass("page-active")
```

CSS：`.page-item { display:none }`，`.page-active { display:block }`。

### 3.3 音视频

优先在每个 frame 里找正在播放、面积最大的 `video`/`audio`（含 open Shadow DOM），设 `fastSeek` / `currentTime`。  
并尝试西瓜播放器 `.xgplayer.player.seek`、YouTube `#movie_player.seekTo`。  
与 Edge 自带媒体条类似，不依赖页面「快进」按钮。

---

## 4. 模块职责

| 文件 | 世界 | 职责 |
|---|---|---|
| `content.js` | isolated | 按钮检测、浮层、拖拽、toast、`applyPageItemNav`、port、HELLO |
| `course-bridge.js` | MAIN | 只处理课件 `UR_COURSE_NAV`，点真实按钮 |
| `background.js` | SW | `frameBook` / `framePorts`、按 frame 注入、主世界执行翻页/seek |
| `overlay.css` | Shadow | 遥控外观 |

`background.js` 里传给 `executeScript` 的函数必须**自包含**，不能闭包引用 SW 里的其它函数。

---

## 5. 消息一览

| 方向 | type | 含义 |
|---|---|---|
| iframe → SW | `UR_FRAME_HELLO` | 报到：href、是否有 `.page-item` |
| 浮层 → SW | `UR_PAGE_ITEM` | 课件翻页（`dir`, `force`） |
| 浮层 → SW | `UR_MEDIA` | 快进/暂停 |
| 浮层 → SW | `UR_BROADCAST` | 转发到其它 frame |
| 顶层 → iframe | `postMessage` `__ur` + `UR_COURSE_NAV` | 跨域通知课件点按钮 |
| iframe → 顶层 | `UR_COURSE_NAV_OK` | 课件已处理 |
| port `ur-frame` | `NAV` / `MEDIA` | 不依赖 frameId 的长连接 |

已知坑：`scripting.executeScript` 进 `mcwk.mycourse.cn` 可能报  
`Extension manifest must request permission to access the respective host`。  
用户需在扩展详情把「网站访问」设为**在所有网站上**。  
`weiban.mycourse.cn` 不要当成课件 frame（用 `mcwk` 或路径 `/course/` 判断）。

---

## 6. 改 UI

- 结构：`content.js` 的 `mount()`
- 样式：只改 `content/overlay.css`，类名一律 `.ur-*`
- 绿点 = 识别到页面按钮；蓝点 = 能直接控播放器
- 强制按钮必须是 toast 里的 `.ur-toast-force`，不要绑回「下一页」

改完把 `manifest.json` 和 `UR.VERSION` 一起加版本号。

---

## 7. 常见故障

| 现象 | 原因 | 处理 |
|---|---|---|
| 浮层版本不是最新 | 只刷新了网页 | `edge://extensions` 重新加载，课件标签关掉重开 |
| 强制 `not_found` `course=0` | 进不了课件 iframe | 网站访问=所有网站 |
| 强制 `Cannot access contents of the page` | 主世界注入被拒 | 同上；隔离世界 port / 改 class 仍可试 |
| 画面翻了但进度没了 | 走了强制切 class | 用普通下一页，走 jQuery |
| 连点下一页误强制 | 旧版 8 秒内再点会强制 | ≥1.2.5 已取消，只认 toast 红按钮 |

---

## 8. 安装（开发）

1. `edge://extensions/` → 开发人员模式  
2. 加载解压缩 → 选本文件夹  
3. 网站访问：**在所有网站上**  
4. 打开任意页，左下角应出现 **万能遥控 v…**

`demo.html` 可测识别绿点、隐藏按钮、确认提示。用 `file://` 时需允许访问文件网址。
